name: C/C++ CI

on:
  push:
      branches: [ main ]

jobs:
  build-and-test:
      runs-on: ubuntu-latest
      steps:
      - uses: actions/checkout@v3
      - name: make libprague
        run: make libprague 
      - name: make all
        run: make all
